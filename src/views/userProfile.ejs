<!DOCTYPE html>
<html lang="en">
<%- include('partials/head')%>
<body>
<%- include('partials/header')%>
<div class="userMancha">
    <img src="/images/manchas/pngwing.com (5).png" alt="">
</div>
    <main class="userProfile">
      <div class="avatar">
            <img src="/images/usuarios/<%= user.avatar %>" alt="">
        </div>
        <div class="userProfile-container">
            <div class="userProfile_box">
                <h3 class="userProfile_title">Datos personales</h3>
                <p class="userProfile_item">Nombre: <%= user.name %></p>
                <p class="userProfile_item">Email: <%= user.email %></p>
                <p class="userProfile_item">Teléfono: <%= user.phone ? user.phone : "------" %></p>
            </div>
            <div class="userProfile_box">
                <h3 class="userProfile_title">Datos de contacto</h3>
                <p class="userProfile_item">Dirección: <%= address !== null && address.street ? address.street : "------" %>></p>
                <p class="userProfile_item">Código postal: <%= address !== null && address.postal_code ? address.postal_code : "------" %></p>
                <p class="userProfile_item">Provincia: <%= address !== null && address.province ? address.province : "------" %></p>
                <p class="userProfile_item">Localidad: <%= address !== null && address.city ? address.city : "------" %></p>
            </div>
            <a href="/users/profile/edit/<%= user.id %>" class="btn btn-ad-primary">EDITAR DATOS</a>
        </div> 
    </main>
<%- include('partials/footer')%>
</body>
</html>